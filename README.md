# Calendar

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 11.2.6.

## Development server

Run `ng serve` for a frontend part. Navigate to `http://localhost:4200/`.
Project use JSON-server for mock up data. To start json-server use `npm run startServer` command.

